const sumCubes = require('../sum-cubes')

test('2 --> 9', () => {
  const n = 2
  const expectedValue = 9
  const actualValue = sumCubes(n)
  expect(actualValue).toEqual(expectedValue)
})
test('3 --> 36', () => {
  const n = 2
  const expectedValue = 9
  const actualValue = sumCubes(n)
  expect(actualValue).toEqual(expectedValue)
})
